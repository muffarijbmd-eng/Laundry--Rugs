// app/layout.tsx
// Content migrated from layout.tsx

import React from 'react';

const Layout = ({ children }) => {
  return (
    <html>
      <body>{children}</body>
    </html>
  );
};

export default Layout;